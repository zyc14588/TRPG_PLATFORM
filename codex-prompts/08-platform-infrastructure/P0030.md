> [v2.21 自包含来源清理标记]
> 原始路径：`codex-prompts/08-platform-infrastructure/P0030.md`
> 筛选状态：`active-prompt`
> 清理日期：2026-07-01
> 使用规则：当前可引用：可由 Codex 读取并参与施工，但必须服从顶层设计与 v2.21 阶段门禁。
> 过时信息处理：正文中出现的 `V4`、`V5`、早期 audit/fix/report 标题、源文档 hash、旧中间路径与历史版本流水仅表示 provenance，不得作为当前产品范围、命名规则或验收标准。若与顶层设计、`AGENTS.md`、`CODEX_STANDALONE_BOOTSTRAP_PROMPT.md`、`SOURCE_BUNDLE_INTEGRATION_GUIDE.md` 或 `V1_ACCEPTANCE_EVIDENCE_MATRIX.md` 冲突，以后者为准。

> [v2.21 当前执行规范化覆盖]
> 执行任何 batch、category prompt 或 per-file prompt 前，必须先读取 `docs/codex/00-index/CURRENT_NORMALIZED_PROMPT_EXECUTION_MAP.md`、`docs/codex/00-index/CURRENT_SAFE_MODULE_AND_OUTPUT_MAP.md` 与 `docs/codex/00-index/CURRENT_TOKEN_REWRITE_TABLE.md`。
> 正文中的 V3/V4/V5/V6、v3/v4/v5/v6、legacy、fix-history、旧 manifest、旧 report、旧 hash 与旧中间路径仅保留为 provenance。任何 Rust module、输出文件、migration、event schema、NATS subject、metric label、测试名或验收入口必须采用 v2.21 normalized current-safe 名称。

# Codex 持久化施工提示词 — 对象存储 — V5 可编码级实现拆分

> 生成日期：2026-06-30
> Prompt ID：`CODEX-0745-08-PLATFORM-INFRASTRUCTURE-9867e80318`
> 源文件：`docs/implementation/90-traceability/per-file-code-ready/08-platform-infrastructure/docs-implementation-08-platform-infrastructure-object-storage-1f964817d1.v5-code-ready.md`
> 源 SHA256：`23b926d515ebcd228c462ba3947935a208b1adb5ecd313806e561e0780bc3e5f`
> 源大小：16603 bytes / 418 lines
> 归档分类：`08-platform-infrastructure` — Deployment / object storage / observability / workers
> 建议 crate：`trpg-platform`
> 建议 Rust module：`platform_infrastructure::object_storage`
> 任务类型：`product-code`
> 施工边界：supplemental 需求归并施工；只生成补充约束 Markdown，不创建 Rust src/test 输出。
> 读取状态：已读取完整文件内容并提取施工锚点。

## 0. 严格修复后的任务类型与边界

- 任务类型：`product-code`
- 输出归并角色：`supplemental-requirement`。
- 权威 crate：`trpg-platform`。
- 稳定 Rust module：`platform_infrastructure::object_storage`；module 名不得携带生成用 hash 后缀、源文件路径片段、历史施工版本片段或截断尾缀。
- Prompt ID、源 SHA256、Prompt 文件名中的 hash 只用于追踪，不得转写为代码、测试名、migration、event schema、NATS subject 或 metric label。
- 施工边界：supplemental 需求归并施工；只生成补充约束 Markdown，不创建 Rust src/test 输出。

## 0.1 Rust module 文件布局策略（第三次严格修复）

- 本任务不是 Rust 输出 owner；Rust 文件布局由 primary prompt 决定。
- 本任务不得声明 flat module 或 directory mod module 输出文件。
- 需要影响代码时，只能写入 merge instruction，由 primary prompt 执行。
- 禁止把源文档路径、旧 V5 中间文件名、截断路径、版本流水或 hash 片段转写为 Rust 输出文件。

## 0.2 输出路径一致性与语义归并策略（第五次严格修复）

- Primary implementation prompt：`CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144`。
- 共享实现 module：`platform_infrastructure::object_storage`。
- 本 Prompt 只维护补充需求文件：`codex-prompts/08-platform-infrastructure/P0030.md`。
- 需要影响代码时，只能把约束归并给 Primary Prompt：`CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144`；不得在本 Prompt 中创建或修改 Rust src/test 文件。
- 禁止从源文档路径、旧 V5 中间文件名、截断路径、版本流水或 hash 片段生成 Rust 输出文件。

## 0.3 Supplemental 语义一致性策略（第五次严格修复）

- `supplemental-requirement` Prompt 只能产生补充约束 Markdown，不得包含创建或实现 Rust module 的可执行指令。
- Supplemental 需要影响代码时，必须写明归并目标 Primary Prompt，并把代码修改交由 primary prompt 执行。
- Primary Prompt 是唯一可以声明 concrete Rust src/test 输出路径的 Prompt。
- 最终校验必须同时检查输出路径、正文施工语义、source-path-like 名称和短 hash 残片。
## 1. 持久化上下文

本提示词用于 Codex CLI / Codex Cloud / Codex IDE 的单文件或小批次施工。执行前先阅读：

```text
AGENTS.md
docs/codex/00-index/codex-persistent-context.md
docs/codex/00-index/codex-execution-map.md
本提示词文件
```

不得把本提示词当作一次性聊天建议；它是可复用的施工任务契约。Codex 在上下文压缩、换线程或分批执行后，必须重新载入本提示词和上面的持久化上下文。

## 2. 不可变施工原则

- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。

## 3. 源文档结构摘要

### 3.1 标题结构

- H1: 对象存储 — V5 可编码级实现拆分
- H2: 1. 施工原则与不可变约束
- H2: 2. 源文档读取摘要
- H3: 标题结构摘录
- H3: 推荐提示词 / 深度拆分 / 调研方向摘录
- H2: 3. 模块边界
- H2: 4. Rust module / trait / struct / enum
- H2: 5. SQLx migration SQL
- H2: 6. API、WebSocket 与事件契约
- H3: OpenAPI / WebSocket / Event Schema
- H2: 7. Command -> Workflow -> Decision -> Event Store 流程
- H2: 8. 权限、可见性与事实来源传播
- H2: 9. 错误码、幂等键、重试与一致性
- H2: 10. 实现任务切片
- H2: 11. 测试、集成验证与最小 Rust POC
- H3: 测试、POC 与验收
- H2: 12. 本文件在 V5 文档组中的归档结论

### 3.2 代码块语言

```text
text×2, ##×5, rust×3, plain×3, sql×1, yaml×1, json×1, mermaid×1, |×1
```

### 3.3 源文档关键锚点

- > 输入文件：`docs/implementation/08-platform-infrastructure/object-storage.md`
- > 目标目录：`docs/implementation/08-platform-infrastructure/`
- ## 1. 施工原则与不可变约束
- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。
- - 分类：`08-platform-infrastructure`，crate：`trpg-platform`。
- - 处理结论：本文件内容已纳入 V5 可编码拆分，输出 Rust 类型、SQLx migration、契约、测试与 POC 任务。
- \| Rust module \| `platform_infrastructure::object_storage` \|
- \| 写入口 \| `/api/v1/internal/platform/commands` \|
- \| Event Store 表 \| `platform_events` \|
- \| NATS / Realtime \| `trpg.platform.worker.started, trpg.platform.metric.recorded` \|
- \| 正史边界 \| 只允许 Event Store 持久化正式事实；Projection、Cache、RAG index 均可重建。 \|
- ## 4. Rust module / trait / struct / enum
- use async_trait::async_trait;
- use sqlx::{PgPool, Postgres, Transaction};
- pub enum AuthorityMode { HumanKp, AiKp }
- pub enum VisibilityLabel { Public, Party, KeeperOnly, InvestigatorPrivate, SystemPrivate }
- pub struct FactProvenance {
- pub struct CommandEnvelope<T> {
- pub command_id: Uuid,
- pub idempotency_key: String,
- pub expected_version: i64,

### 3.4 深度拆分 / 调研 / POC 摘录

- ### 推荐提示词 / 深度拆分 / 调研方向摘录
- - 本文件未显式提供推荐提示词或调研方向；本轮继承 `architecture/document-set.md` 的完成标准与 V5 默认可编码级输出清单。
- ## 11. 测试、集成验证与最小 Rust POC
- ### 测试、POC 与验收
- [source code fence: rust]
- #[tokio::test]
- async fn object_storage_rejects_authority_contract_violation() {
- // Arrange: 构造 AI_KP campaign 下的人类 KP 写命令，或 HUMAN_KP 下的 AI 正式写命令。
- // Act: 调用 command handler。
- // Assert: 返回 TrpgError::AuthorityViolation，且 Event Store 无新增事件。
- }
- #[tokio::test]
- async fn object_storage_keeps_visibility_and_fact_provenance_on_replay() {
- // Arrange: 写入含 KeeperOnly/SystemPrivate 的事件序列。
- // Act: 重建 projection / RAG index / websocket delta。
- // Assert: principal 不具备权限时不可见，projection hash 与 golden fixture 一致。
- }
- [source code fence: plain]
- \| 测试层级 \| 必须覆盖 \|
- \|---\|---\|
- \| Unit \| Authority Guard、Visibility lattice、Fact Provenance copy-on-write、错误码映射。 \|
- \| Integration \| SQLx migration、expected_version 冲突、idempotency key 重放、OpenFGA/OPA deny。 \|
- \| Contract \| OpenAPI DTO、Event JSON Schema、NATS subject、WebSocket delta。 \|
- \| Golden Scenario \| 从 Command 到 Event Store 再到 Projection 的可重放一致性。 \|
- \| POC 命令 \| `cargo test -p trpg-platform --test object_storage_contract_smoke -- --nocapture`。 \|
- 观测指标：`trpg_command_total{module="object_storage"}`、`trpg_event_append_latency_ms`、`trpg_policy_deny_total`、`trpg_projection_lag_events`、`trpg_visibility_redaction_total`。

### 3.5 检测到的契约元素

| 元素 | 摘录 |
|---|---|
| Rust items | AuthorityMode, VisibilityLabel, FactProvenance, CommandEnvelope, DecisionEnvelope, TrpgError, StartWorkerPayload, RecordMetricPayload, RotateObjectPayload, ApplyDeploymentConfigPayload, ObjectStorage1f964817d1Command, WorkerStartedPayload, MetricRecordedPayload |
| SQL / migration | platform_events, object_storage_outbox |
| API endpoint | /api/v1/internal/platform/commands |
| NATS / realtime | trpg.platform.worker.started, trpg.platform.metric.recorded, trpg.object.storage.1f964817d1.v5.event.schema.json |
| 模板债务 | 未发现旧模板债务。 |

## 4. Codex 编码提示词（可直接复制）

```text
你是 Codex，正在执行 supplemental requirement 归并任务。

任务 ID：CODEX-0745-08-PLATFORM-INFRASTRUCTURE-9867e80318
源文档：docs/implementation/90-traceability/per-file-code-ready/08-platform-infrastructure/docs-implementation-08-platform-infrastructure-object-storage-1f964817d1.v5-code-ready.md
目标分类：08-platform-infrastructure — Deployment / object storage / observability / workers
权威 crate：trpg-platform
归并目标：platform_infrastructure::object_storage
Primary Prompt：CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144
任务类型：product-code
输出归并角色：supplemental-requirement

目标：
1. 从源文档中提取对 `platform_infrastructure::object_storage` 有价值的补充约束、测试断言、验收项、错误码、观测指标、权限/可见性要求和风险说明。
2. 只维护补充需求 Markdown：`codex-prompts/08-platform-infrastructure/P0030.md`。
3. 需要影响代码时，把约束归并给 Primary Prompt `CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144`，由 primary 任务统一处理 Rust 输出文件。
4. 不得声明、修改或要求新增 Rust src/test 输出文件；不得在本任务中生成 migration、API handler、NATS subject 或正式状态写入路径。

执行顺序：
1. 读取 AGENTS.md、docs/codex/00-index/codex-persistent-context.md、docs/codex/00-index/codex-execution-map.md，以及本任务提示词。
2. 阅读源文档锚点，只抽取补充约束，不执行代码变更。
3. 在 `codex-prompts/08-platform-infrastructure/P0030.md` 中写入：约束摘要、必须合并到 primary 的事项、测试断言、风险、追踪引用、未决问题。
4. 在补充文档顶部声明 Primary Prompt：`CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144`，并声明本任务不拥有 Rust 输出路径。
5. 若发现 primary prompt 缺少必要施工约束，只在 supplemental Markdown 中记录 merge instruction，不直接编辑 primary Rust 文件。

建议输出文件：
codex-prompts/08-platform-infrastructure/P0030.md

完成后输出：
- 补充需求文档路径。
- 需要 primary prompt 合并的约束清单。
- 建议新增或强化的测试断言清单。
- 未决问题和风险。
```

## 5. Codex 测试提示词（可直接复制）

```text
你是 Codex 测试代理，负责审查 supplemental 任务 CODEX-0745-08-PLATFORM-INFRASTRUCTURE-9867e80318 的补充需求是否可被 primary prompt 合并。

测试目标：
1. 验证 supplemental 文档没有声明 Rust src/test 输出路径。
2. 验证 supplemental 文档明确指向 Primary Prompt，并列出可合并约束。
3. 验证补充约束没有削弱 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance 或 Policy Gate。
4. 验证补充测试断言可以映射到 primary 的 contract / integration / replay / golden 测试。

必须执行或补齐的检查：
- grep 检查本 supplemental 文档不得出现 concrete Rust src/test 输出路径作为本任务输出。
- 检查 Primary Prompt ID、源文件、源 SHA 和本任务 ID 是否齐全。
- 检查补充约束是否以 P0/P1/P2 或 must/should/could 分级。
- 若与 primary 合并执行，再运行 primary prompt 指定的 cargo fmt / clippy / test。
```

## 6. Codex Review / Repair 提示词

```text
审查 supplemental 任务 CODEX-0745-08-PLATFORM-INFRASTRUCTURE-9867e80318：
1. 查找是否把 supplemental 误写成 primary 代码施工。
2. 查找是否声明 Rust src/test 输出文件、migration、API handler、NATS subject 或正式状态写入路径。
3. 查找是否缺少 Primary Prompt 归并目标。
4. 查找是否存在削弱施工原则的表述。
5. 若发现需要代码改动，只输出 merge instruction，交由 Primary Prompt 执行。
```

## 7. 验收门禁

| 门禁 | 要求 |
|---|---|
| 归并角色 | 本 Prompt 必须保持 `supplemental-requirement`。 |
| Primary 归属 | 所有实现约束必须归并到 `CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144`。 |
| 补充文档 | 必须维护 `codex-prompts/08-platform-infrastructure/P0030.md`。 |
| 施工原则 | 不得弱化 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance 或 Policy Gate。 |
| 可追踪性 | supplemental 文档必须引用源文件 `docs/implementation/90-traceability/per-file-code-ready/08-platform-infrastructure/docs-implementation-08-platform-infrastructure-object-storage-1f964817d1.v5-code-ready.md` 与 Prompt ID `CODEX-0745-08-PLATFORM-INFRASTRUCTURE-9867e80318`。 |
| 测试 | 只提交补充测试断言；实际代码测试由 primary prompt 执行。 |

## 8. 建议落地文件

本 Prompt 是 `CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144` 的 supplemental requirement；不得声明或创建 primary Rust 输出文件。
只维护下列补充需求追踪文档，或在与 primary prompt 合并执行时把约束并入 primary 实现。

- `codex-prompts/08-platform-infrastructure/P0030.md`

### 输出路径一致性门禁

- `建议 crate` 必须保持为 `trpg-platform`。
- `建议 Rust module` 必须保持为 `platform_infrastructure::object_storage`。
- 若需要影响 `platform_infrastructure::object_storage`，必须经 primary prompt `CODEX-0077-08-PLATFORM-INFRASTRUCTURE-f2b7a72144` 归并。
- 本任务不拥有 Rust src/test 输出路径。
