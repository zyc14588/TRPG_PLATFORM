> [v2.21 自包含来源清理标记]
> 原始路径：`codex-prompts/09-security-governance/P0054.md`
> 筛选状态：`active-prompt`
> 清理日期：2026-07-01
> 使用规则：当前可引用：可由 Codex 读取并参与施工，但必须服从顶层设计与 v2.21 阶段门禁。
> 过时信息处理：正文中出现的 `V4`、`V5`、早期 audit/fix/report 标题、源文档 hash、旧中间路径与历史版本流水仅表示 provenance，不得作为当前产品范围、命名规则或验收标准。若与顶层设计、`AGENTS.md`、`CODEX_STANDALONE_BOOTSTRAP_PROMPT.md`、`SOURCE_BUNDLE_INTEGRATION_GUIDE.md` 或 `V1_ACCEPTANCE_EVIDENCE_MATRIX.md` 冲突，以后者为准。

> [v2.21 当前执行规范化覆盖]
> 执行任何 batch、category prompt 或 per-file prompt 前，必须先读取 `docs/codex/00-index/CURRENT_NORMALIZED_PROMPT_EXECUTION_MAP.md`、`docs/codex/00-index/CURRENT_SAFE_MODULE_AND_OUTPUT_MAP.md` 与 `docs/codex/00-index/CURRENT_TOKEN_REWRITE_TABLE.md`。
> 正文中的 V3/V4/V5/V6、v3/v4/v5/v6、legacy、fix-history、旧 manifest、旧 report、旧 hash 与旧中间路径仅保留为 provenance。任何 Rust module、输出文件、migration、event schema、NATS subject、metric label、测试名或验收入口必须采用 v2.21 normalized current-safe 名称。

# Codex 持久化施工提示词 — strict_rework_audit.json.md — V5 可编码级实现拆分

> 生成日期：2026-06-30
> Prompt ID：`CODEX-0838-09-SECURITY-GOVERNANCE-a6f388563f`
> 源文件：`docs/implementation/90-traceability/per-file-code-ready/09-security-governance/strict-rework-audit-json-b89ad9eac0.v5-code-ready.md`
> 源 SHA256：`e67196f5ba771eb88e393511365a3710ca4d5e1bedae41e2c22ae8e62427523e`
> 源大小：16977 bytes / 416 lines
> 归档分类：`09-security-governance` — OpenFGA / OPA / audit / privacy / visibility enforcement
> 建议 crate：`trpg-security-governance`
> 建议 Rust module：`security_governance::strict_rework_audit`
> 任务类型：`traceability-maintenance`
> 施工边界：文档治理 / 追踪维护施工；只维护 Markdown、索引、矩阵、报告或验证清单，不创建业务域代码。
> 读取状态：已读取完整文件内容并提取施工锚点。

## 0. 严格修复后的任务类型与边界

- 任务类型：`traceability-maintenance`
- 输出归并角色：`documentation-or-traceability`。
- 权威 crate：`trpg-security-governance`。
- 稳定 Rust module：`security_governance::strict_rework_audit`；module 名不得携带生成用 hash 后缀、源文件路径片段、历史施工版本片段或截断尾缀。
- Prompt ID、源 SHA256、Prompt 文件名中的 hash 只用于追踪，不得转写为代码、测试名、migration、event schema、NATS subject 或 metric label。
- 施工边界：文档治理 / 追踪维护施工；只维护 Markdown、索引、矩阵、报告或验证清单，不创建业务域代码。

## 0.1 Rust module 文件布局策略（第三次严格修复）

- 本任务不是业务 Rust 输出 owner。
- 文档治理、traceability、ops runbook 的非 primary 任务不得创建业务 Rust module。
- 如需记录代码影响，只能创建 Markdown 追踪、补充需求或治理说明。

## 0.2 输出路径一致性与语义归并策略（第五次严格修复）

- 本任务类型默认不创建业务 Rust 输出文件。
- 建议输出文件只允许落在 Markdown 文档、索引、矩阵、报告或追踪更新路径中。
- 禁止从源文档路径、旧 V5 中间文件名、截断路径或 hash 片段生成 Rust module 文件。
## 1. 持久化上下文

本提示词用于 Codex CLI / Codex Cloud / Codex IDE 的单文件或小批次施工。执行前先阅读：

```text
AGENTS.md
docs/codex/00-index/codex-persistent-context.md
docs/codex/00-index/codex-execution-map.md
本提示词文件
```

不得把本提示词当作一次性聊天建议；它是可复用的施工任务契约。Codex 在上下文压缩、换线程或分批执行后，必须重新载入本提示词和上面的持久化上下文。

## 2. 不可变施工原则

- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。

## 3. 源文档结构摘要

### 3.1 标题结构

- H1: strict_rework_audit.json.md — V5 可编码级实现拆分
- H2: 1. 施工原则与不可变约束
- H2: 2. 源文档读取摘要
- H3: 标题结构摘录
- H3: 推荐提示词 / 深度拆分 / 调研方向摘录
- H2: 3. 模块边界
- H2: 4. Rust module / trait / struct / enum
- H2: 5. SQLx migration SQL
- H2: 6. API、WebSocket 与事件契约
- H3: OpenAPI / WebSocket / Event Schema
- H2: 7. Command -> Workflow -> Decision -> Event Store 流程
- H2: 8. 权限、可见性与事实来源传播
- H2: 9. 错误码、幂等键、重试与一致性
- H2: 10. 实现任务切片
- H2: 11. 测试、集成验证与最小 Rust POC
- H3: 测试、POC 与验收
- H2: 12. 本文件在 V5 文档组中的归档结论

### 3.2 代码块语言

```text
text×2, ##×5, rust×3, plain×3, sql×1, yaml×1, json×1, mermaid×1, |×1
```

### 3.3 源文档关键锚点

- ## 1. 施工原则与不可变约束
- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。
- - 处理结论：本文件内容已纳入 V5 可编码拆分，输出 Rust 类型、SQLx migration、契约、测试与 POC 任务。
- \| Aggregate \| `PolicyDecision` \|
- \| Rust module \| `security_governance::strict_rework_audit_json` \|
- \| Event Store 表 \| `policy_events` \|
- \| NATS / Realtime \| `trpg.policy.decision.recorded, trpg.audit.entry.written` \|
- \| 正史边界 \| 只允许 Event Store 持久化正式事实；Projection、Cache、RAG index 均可重建。 \|
- ## 4. Rust module / trait / struct / enum
- use async_trait::async_trait;
- use sqlx::{PgPool, Postgres, Transaction};
- pub enum AuthorityMode { HumanKp, AiKp }
- pub enum VisibilityLabel { Public, Party, KeeperOnly, InvestigatorPrivate, SystemPrivate }
- pub struct FactProvenance {
- pub struct CommandEnvelope<T> {
- pub command_id: Uuid,
- pub idempotency_key: String,
- pub expected_version: i64,
- pub correlation_id: Uuid,
- pub causation_id: Option<Uuid>,
- pub visibility: VisibilityLabel,

### 3.4 深度拆分 / 调研 / POC 摘录

- ### 推荐提示词 / 深度拆分 / 调研方向摘录
- - 本文件未显式提供推荐提示词或调研方向；本轮继承 `architecture/document-set.md` 的完成标准与 V5 默认可编码级输出清单。
- ## 11. 测试、集成验证与最小 Rust POC
- ### 测试、POC 与验收
- [source code fence: rust]
- #[tokio::test]
- async fn strict_rework_audit_json_rejects_authority_contract_violation() {
- // Arrange: 构造 AI_KP campaign 下的人类 KP 写命令，或 HUMAN_KP 下的 AI 正式写命令。
- // Act: 调用 command handler。
- // Assert: 返回 TrpgError::AuthorityViolation，且 Event Store 无新增事件。
- }
- #[tokio::test]
- async fn strict_rework_audit_json_keeps_visibility_and_fact_provenance_on_replay() {
- // Arrange: 写入含 KeeperOnly/SystemPrivate 的事件序列。
- // Act: 重建 projection / RAG index / websocket delta。
- // Assert: principal 不具备权限时不可见，projection hash 与 golden fixture 一致。
- }
- [source code fence: plain]
- \| 测试层级 \| 必须覆盖 \|
- \|---\|---\|
- \| Unit \| Authority Guard、Visibility lattice、Fact Provenance copy-on-write、错误码映射。 \|
- \| Integration \| SQLx migration、expected_version 冲突、idempotency key 重放、OpenFGA/OPA deny。 \|
- \| Contract \| OpenAPI DTO、Event JSON Schema、NATS subject、WebSocket delta。 \|
- \| Golden Scenario \| 从 Command 到 Event Store 再到 Projection 的可重放一致性。 \|
- \| POC 命令 \| `cargo test -p trpg-security-governance --test strict_rework_audit_json_contract_smoke -- --nocapture`。 \|
- 观测指标：`trpg_command_total{module="strict_rework_audit_json"}`、`trpg_event_append_latency_ms`、`trpg_policy_deny_total`、`trpg_projection_lag_events`、`trpg_visibility_redaction_total`。

### 3.5 检测到的契约元素

| 元素 | 摘录 |
|---|---|
| Rust items | AuthorityMode, VisibilityLabel, FactProvenance, CommandEnvelope, DecisionEnvelope, TrpgError, CheckRelationshipAuthzPayload, EvaluateContextPolicyPayload, RedactByVisibilityPayload, WriteAuditEntryPayload, StrictReworkAuditJsonB89ad9eac0Command, RelationshipAuthzCheckedPayload, ContextPolicyEvaluatedPayload |
| SQL / migration | policy_events, strict_rework_audit_json_outbox |
| API endpoint | /api/v1/internal/policy/check |
| NATS / realtime | trpg.policy.decision.recorded, trpg.audit.entry.written, trpg.strict.rework.audit.json.b89ad9eac0.v5.event.schema.json |
| 模板债务 | 未发现旧模板债务。 |


## 4. Codex 编码提示词（可直接复制）

```text
你是 Codex，正在 COC AI TRPG 文档组中执行 documentation / traceability 维护任务。

任务 ID：CODEX-0838-09-SECURITY-GOVERNANCE-a6f388563f
源文档：docs/implementation/90-traceability/per-file-code-ready/09-security-governance/strict-rework-audit-json-b89ad9eac0.v5-code-ready.md
目标分类：09-security-governance — OpenFGA / OPA / audit / privacy / visibility enforcement
建议 crate：trpg-security-governance
治理 module：security_governance::strict_rework_audit
任务类型：traceability-maintenance
输出归并角色：documentation-or-traceability
施工边界：文档治理/追踪维护；不得创建业务 Rust source/test 输出文件。

目标：
1. 维护 strict rework audit 的 Markdown 追踪证据、矩阵或校验清单。
2. 将任何代码层约束转写为 primary/supplemental prompt 的验收项，不在本 Prompt 中直接要求代码变更。
3. 保持 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance、Policy Gate 等施工原则可追踪。

执行顺序：
1. 读取 AGENTS.md、docs/codex/00-index/codex-persistent-context.md、docs/codex/00-index/codex-execution-map.md，以及本任务提示词。
2. 维护 `docs/codex/09-security-governance/strict_rework_audit.md` 或相关索引/矩阵/报告；不要创建业务 Rust source/test 文件。
3. 若发现应影响代码的约束，记录到 traceability 矩阵，并链接到对应 primary/supplemental prompt。
4. 不得把源文档路径、旧 V5 中间文件名、截断路径或 hash 片段转写为 Rust module、Rust 文件名、测试名、migration、event schema、NATS subject 或 metric label。

建议输出文件：
docs/codex/09-security-governance/strict_rework_audit.md

完成后输出：
- 文档/索引/矩阵变更清单。
- 关联的 Prompt ID 与源文件路径。
- 需要转交给 primary/supplemental prompt 的代码约束。
- 未解决的追踪缺口或人工确认项。
```

## 5. Codex 测试提示词（可直接复制）

```text
验证本 Prompt 只维护 strict rework audit 的 Markdown/traceability 资产，不声明业务 Rust source/test 输出文件，并保持 Prompt ID、源路径、SHA 与 Manifest 追踪一致。
```

## 6. Codex Review / Repair 提示词

```text
审查任务 CODEX-0838-09-SECURITY-GOVERNANCE-a6f388563f 的文档治理变更：确认没有误要求创建业务 Rust module、migration、API handler、NATS subject 或 Event Store schema；确认所有代码约束均转交 primary/supplemental prompt。
```

## 7. 验收门禁

| 门禁 | 要求 |
|---|---|
| 文档完成 | `docs/codex/09-security-governance/strict_rework_audit.md` 或相关索引/矩阵/报告已更新。 |
| 无业务代码 | 本 Prompt 不声明 Rust source/test 输出文件。 |
| 追踪完整 | Prompt ID、源文件、源 SHA、输出路径、角色和边界一致。 |

## 8. 建议落地文件

- `docs/codex/09-security-governance/strict_rework_audit.md`
