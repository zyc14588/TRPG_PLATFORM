# Codex Active Normalized Overlay — v2.21

This directory contains the current execution overlay for prompt-to-module and prompt-to-output normalization.

Current files:

```text
CURRENT_NORMALIZED_PROMPT_EXECUTION_MAP.md
CURRENT_SAFE_MODULE_AND_OUTPUT_MAP.md
```

The canonical token rewrite table is intentionally not duplicated here. Use:

```text
docs/codex/00-index/CURRENT_TOKEN_REWRITE_TABLE.md
```

Any legacy token table alias is provenance-only under `source-archive/**`.
