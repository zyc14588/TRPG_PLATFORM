> [V2.1 原始路径追踪副本]
> 原始路径：`codex-prompts/02-domain-core/docs-implementation-90-traceability-per-file-code-ready-02-domain-core-sources-v3-baseline-document-group-docs-domain-character-combat-san-chase-1b674-127e67bc59.codex-prompt.md`
> 筛选状态：`path-provenance-only`
> 当前状态：该原始路径因含旧版本、旧 hash、中间产物或历史命名片段，不作为 Codex 当前执行入口；当前执行必须使用 `docs/codex/00-index/CURRENT_NORMALIZED_PROMPT_EXECUTION_MAP.md`、`docs/codex/00-index/CURRENT_SAFE_MODULE_AND_OUTPUT_MAP.md` 与 `docs/codex/00-index/CURRENT_TOKEN_REWRITE_TABLE.md` 中的 current-safe 名称。
> 内容处理：本副本仅用于证明已遍历和可追溯；正文历史版本词、旧 module、旧输出路径和旧 hash 不得转写为工程实现。

# Codex 持久化施工提示词 — 角色卡、战斗、SAN 与追逐详细设计 — V5 可编码级实现拆分

> 生成日期：2026-06-30
> Prompt ID：`CODEX-0310-02-DOMAIN-CORE-127e67bc59`
> 源文件：`docs/implementation/90-traceability/per-file-code-ready/02-domain-core/sources-v3-baseline-document-group-docs-domain-character-combat-san-chase-1b6743fecf.v5-code-ready.md`
> 源 SHA256：`4426e6f76327a966fce57a9b47c9f05cfd5eeb1aaa193f6e09967b09be749982`
> 源大小：19892 bytes / 497 lines
> 归档分类：`02-domain-core` — Domain core / command / authority / visibility
> 建议 crate：`trpg-domain-core`
> 建议 Rust module：`domain_core::character_combat_san_chase`
> 任务类型：`product-code`
> 施工边界：primary 实现施工；只能按权威 crate/module 创建或修改唯一 Rust src/test 输出文件，并保持 Command -> Workflow -> Decision -> Event Store 约束。
> 读取状态：已读取完整文件内容并提取施工锚点。

## 0. 严格修复后的任务类型与边界

- 任务类型：`product-code`
- 输出归并角色：`primary-implementation`。
- 权威 crate：`trpg-domain-core`。
- 稳定 Rust module：`domain_core::character_combat_san_chase`；module 名不得携带生成用 hash 后缀、源文件路径片段、V4/V5 施工版本片段或截断尾缀。
- Prompt ID、源 SHA256、Prompt 文件名中的 hash 只用于追踪，不得转写为代码、测试名、migration、event schema、NATS subject 或 metric label。
- 施工边界：primary 实现施工；只能按权威 crate/module 创建或修改唯一 Rust src/test 输出文件，并保持 Command -> Workflow -> Decision -> Event Store 约束。

## 0.1 Rust module 文件布局策略（第三次严格修复）

- 本任务采用单一 Rust module 文件布局；默认选择 flat module 文件。
- 禁止在同一任务中同时创建 flat 文件与 directory mod 文件；两者只能二选一。
- 只有当后续需要拆分多文件子模块时，才允许迁移到目录式布局；迁移时必须删除 flat 文件、更新 `lib.rs`，并在同一变更中补测试与说明。
- Rust 输出文件必须由 module 末段 `character_combat_san_chase` 推导，不得使用源文档路径片段。

## 0.2 输出路径一致性与语义归并策略（第五次严格修复）

- 本 Prompt 是 `domain_core::character_combat_san_chase` 的唯一 primary implementation prompt。
- 建议输出文件必须由权威 crate 与稳定 Rust module 推导，Rust 文件名必须等于 `character_combat_san_chase`。
- 若其他 prompt 也补充 `domain_core::character_combat_san_chase` 的需求，它们只能作为 supplemental requirement，不得重复声明或创建本 Prompt 的 Rust 输出文件。
- 禁止从源文档路径、旧 V5 中间文件名、截断路径或 hash 片段生成新的 Rust 输出文件。
## 1. 持久化上下文

本提示词用于 Codex CLI / Codex Cloud / Codex IDE 的单文件或小批次施工。执行前先阅读：

```text
AGENTS.md
docs/codex/00-index/codex-persistent-context.md
docs/codex/00-index/codex-execution-map.md
本提示词文件
```

不得把本提示词当作一次性聊天建议；它是可复用的施工任务契约。Codex 在上下文压缩、换线程或分批执行后，必须重新载入本提示词和上面的持久化上下文。

## 2. 不可变施工原则

- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。

## 3. 源文档结构摘要

### 3.1 标题结构

- H1: 角色卡、战斗、SAN 与追逐详细设计 — V5 可编码级实现拆分
- H2: 1. 施工原则与不可变约束
- H2: 2. 源文档读取摘要
- H3: 标题结构摘录
- H3: 推荐提示词 / 深度拆分 / 调研方向摘录
- H4: 摘录 1
- H4: 摘录 2
- H4: 摘录 3
- H4: 摘录 4
- H2: 参考来源
- H2: 3. 模块边界
- H2: 4. Rust module / trait / struct / enum
- H2: 5. SQLx migration SQL
- H2: 6. API、WebSocket 与事件契约
- H3: OpenAPI / WebSocket / Event Schema
- H2: 7. Command -> Workflow -> Decision -> Event Store 流程
- H2: 8. 权限、可见性与事实来源传播
- H2: 9. 错误码、幂等键、重试与一致性
- H2: 10. 实现任务切片
- H2: 11. 测试、集成验证与最小 Rust POC

### 3.2 代码块语言

```text
text×6, ##×6, ####×3, rust×3, plain×3, sql×1, yaml×1, json×1, mermaid×1, |×1
```

### 3.3 源文档关键锚点

- ## 1. 施工原则与不可变约束
- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。
- - 检测到施工关键词：`Authority Contract`×1, `Visibility`×1, `Fact Provenance`×1, `Agent Governance`×1, `OpenFGA`×2, `OPA`×2, `NATS`×2, `JetStream`×1, `SQLx`×3, `pgvector`×3, `Golden Scenario`×1。
- - 处理结论：本文件内容已纳入 V5 可编码拆分，输出 Rust 类型、SQLx migration、契约、测试与 POC 任务。
- - H2: 8. Rust 模块
- - H2: 9. 测试策略
- 你是资深 Rust 企业级架构师。请基于本文件继续拆分到实现级设计，输出：
- 1. 具体 Rust module / trait / struct / enum 定义；
- 2. SQLx migration SQL；
- 3. OpenAPI / WebSocket / Event Schema；
- 4. 单元测试、集成测试、Golden Scenario 测试用例；
- 5. 错误码、幂等键、重试策略与观测指标。
- 要求保持 Authority Contract、Visibility、Fact Provenance、Event Sourcing、Agent Governance 不变。
- - 对比 SQLx、SeaORM、Diesel 在当前模块中的适配成本。
- - 对比 NATS JetStream、Kafka、RabbitMQ 在事件投影与实时通知中的语义差异。
- - 验证 OpenFGA + OPA 的本地开发部署、缓存、灰度与回滚方案。
- - 为本模块生成最小可运行 Rust POC。
- POC。
- - SQLx：<https://github.com/transact-rs/sqlx>
- - async-nats / nats.rs：<https://github.com/nats-io/nats.rs>
- - NATS Server：<https://github.com/nats-io/nats-server>

### 3.4 深度拆分 / 调研 / POC 摘录

- ### 推荐提示词 / 深度拆分 / 调研方向摘录
- #### 摘录 1
- [source code fence: text]
- 推荐提示词：
- ~~~text
- 你是资深 Rust 企业级架构师。请基于本文件继续拆分到实现级设计，输出：
- 1. 具体 Rust module / trait / struct / enum 定义；
- 2. SQLx migration SQL；
- 3. OpenAPI / WebSocket / Event Schema；
- 4. 单元测试、集成测试、Golden Scenario 测试用例；
- 5. 错误码、幂等键、重试策略与观测指标。
- 要求保持 Authority Contract、Visibility、Fact Provenance、Event Sourcing、Agent Governance 不变。
- ~~~
- 进一步调研方向：
- - 对比 SQLx、SeaORM、Diesel 在当前模块中的适配成本。
- - 对比 NATS JetStream、Kafka、RabbitMQ 在事件投影与实时通知中的语义差异。
- - 验证 OpenFGA + OPA 的本地开发部署、缓存、灰度与回滚方案。
- - 为本模块生成最小可运行 Rust POC。
- [source code fence: plain]
- ## 11. 测试、集成验证与最小 Rust POC
- ### 测试、POC 与验收
- [source code fence: rust]
- #[tokio::test]
- async fn character_combat_san_chase_rejects_authority_contract_violation() {
- // Arrange: 构造 AI_KP campaign 下的人类 KP 写命令，或 HUMAN_KP 下的 AI 正式写命令。
- // Act: 调用 command handler。
- // Assert: 返回 TrpgError::AuthorityViolation，且 Event Store 无新增事件。
- }
- #[tokio::test]
- async fn character_combat_san_chase_keeps_visibility_and_fact_provenance_on_replay() {
- // Arrange: 写入含 KeeperOnly/SystemPrivate 的事件序列。
- // Act: 重建 projection / RAG index / websocket delta。
- // Assert: principal 不具备权限时不可见，projection hash 与 golden fixture 一致。
- }
- [source code fence: plain]
- \| 测试层级 \| 必须覆盖 \|

### 3.5 检测到的契约元素

| 元素 | 摘录 |
|---|---|
| Rust items | AuthorityMode, VisibilityLabel, FactProvenance, CommandEnvelope, DecisionEnvelope, TrpgError, SubmitCommandPayload, GuardAuthorityContractPayload, RecordDecisionPayload, ForkCanonLineagePayload, CharacterCombatSanChase1b6743fecfCommand, CommandAcceptedPayload, AuthorityContractGuardedPayload |
| SQL / migration | domain_events, character_combat_san_chase_outbox |
| API endpoint | /api/v1/campaigns/{campaign_id}/commands |
| NATS / realtime | trpg.domain.command.accepted, trpg.domain.decision.recorded, trpg.character.combat.san.chase.1b6743fecf.v5.event.schema.json |
| 模板债务 | 未发现旧模板债务。 |

## 4. Codex 编码提示词（可直接复制）

```text
你是 Codex，正在 COC AI TRPG Rust monorepo 中执行 primary 可编码级施工任务。

任务 ID：CODEX-0310-02-DOMAIN-CORE-127e67bc59
源文档：docs/implementation/90-traceability/per-file-code-ready/02-domain-core/sources-v3-baseline-document-group-docs-domain-character-combat-san-chase-1b6743fecf.v5-code-ready.md
目标分类：02-domain-core — 领域核心、Authority、Visibility、Fact Provenance
建议 crate：trpg-domain-core
建议实现单元：domain_core::character_combat_san_chase
任务类型：product-code
输出归并角色：primary-implementation
本 Prompt 是 `domain_core::character_combat_san_chase` 的唯一 Rust src/test 输出 owner。

目标：
1. 按源文档语义完成 `domain_core::character_combat_san_chase` 的可编码实现，并把所有补充 Prompt 的约束合并进同一实现单元。
2. 保持 Authority Contract、KP 互斥、Command -> Workflow -> Decision -> Event Store -> Projection、Visibility、Fact Provenance、Tool Grant / Policy Gate 等约束不可被绕过。
3. 不接受仅有占位类型、serde_json::Value 泛滥或 ModuleService/ModuleCommand/ModuleError 这类模板名作为最终实现。

执行顺序：
1. 读取 AGENTS.md、docs/codex/00-index/codex-persistent-context.md、docs/codex/00-index/codex-execution-map.md，以及本任务提示词。
2. 在仓库中定位 crate `trpg-domain-core`；若不存在，先在 workspace 中创建最小 crate 骨架，并补充 Cargo.toml、lib.rs、错误模型、测试目录。
3. 使用 stable flat module 文件 `crates/trpg-domain-core/src/character_combat_san_chase.rs`；不得同时创建同一 module 的directory-style module 版本。
4. 优先定义领域专名 command/event/payload/service/repository/error 类型，避免裸字符串和无约束 JSON。
5. 所有写入路径必须接收 CommandEnvelope，并校验 idempotency_key、expected_version、actor、authority_mode、visibility、fact_provenance、correlation_id、causation_id。
6. 若涉及正式事实，必须通过 SQLx transaction 追加 Event Store 事件；Projection、Cache、RAG index 只能作为可重建读模型。
7. 若涉及 API，补 Axum handler、utoipa schema、OpenAPI tag、错误响应、幂等 header、request_id/correlation_id 传播。
8. 若涉及 realtime / NATS，补 subject 常量、payload schema、consumer 幂等、outbox 发布与重放安全说明。
9. 若涉及安全，补 OpenFGA relationship check 与 OPA/Rego context policy gate；不得让 Agent、插件或 provider 直连写库。
10. 若涉及 AI，Agent 只能生成 Proposal / ToolCall / DraftDecision；正式状态必须交给 workflow/decision handler 写入 Event Store。
11. 更新或新增单元测试、集成测试、契约测试、replay/golden 测试，并把测试覆盖映射回本任务 ID。

文件布局要求：默认 flat module；禁止同时创建 flat 文件和目录式 mod 文件。

建议输出文件：
crates/trpg-domain-core/src/character_combat_san_chase.rs
crates/trpg-domain-core/tests/character_combat_san_chase_contract_tests.rs

完成后输出：
- 变更文件清单。
- 新增 public API / event / migration 清单。
- 已运行测试命令及结果。
- 未完成项和阻塞项，必须明确标为 TODO，并说明原因。
```

## 5. Codex 测试提示词（可直接复制）

```text
你是 Codex 测试代理，负责验证 primary 任务 CODEX-0310-02-DOMAIN-CORE-127e67bc59 的实现是否满足源文档 `docs/implementation/90-traceability/per-file-code-ready/02-domain-core/sources-v3-baseline-document-group-docs-domain-character-combat-san-chase-1b6743fecf.v5-code-ready.md`。

测试目标：
1. 验证所有写命令都需要 idempotency_key、expected_version、actor、authority_mode、visibility、fact_provenance、correlation_id、causation_id。
2. 验证 HUMAN_KP / AI_KP 互斥、Authority Contract 不可变、AI 不直接写正式状态。
3. 验证 Event Store 是唯一正史，projection/cache/RAG index 可以从事件重建。
4. 验证 visibility label 不泄露，Fact Provenance 在 API/Event/Agent/RAG/Export/Log/Metric 间传播。
5. 验证错误码、重试策略、观测指标、审计日志与契约文档一致。

必须执行或补齐的测试：
- cargo fmt --all -- --check
- cargo clippy --workspace --all-targets --all-features -- -D warnings
- cargo test --workspace --all-features
- cargo test -p trpg-domain-core --all-features
- 若存在 migrations：sqlx migrate run / sqlx migrate revert，并用测试库验证幂等。
- 若存在 OpenAPI：生成 schema 并做快照 diff。
- 若存在 NATS/WebSocket：做消息 schema contract test 和 replay test。
- 若涉及安全：补 unauthorized / forbidden / visibility leak negative tests。

失败处理：
- 不要删除测试来让 CI 通过。
- 不要弱化施工原则。
- 若旧代码不符合约束，先写 regression test，再做最小修复。
```

## 6. Codex Review / Repair 提示词

```text
审查 primary 任务 CODEX-0310-02-DOMAIN-CORE-127e67bc59 的代码变更：
1. 查找绕过 Command -> Workflow -> Decision -> Event Store 的路径。
2. 查找 AI_KP/HUMAN_KP 混用、Authority Contract 原地修改、visibility 泄露、provenance 丢失。
3. 查找泛化模板名、serde_json::Value 域模型滥用、缺少 SQLx transaction、缺少 idempotency 或 expected_version 的 handler。
4. 查找未绑定测试、未更新 OpenAPI/Event schema、未记录审计日志、未补 tracing/metrics 的变更。
5. 查找是否误创建 directory-style Rust module file 或 source-path-like Rust 文件名。
6. 输出 P0/P1/P2 findings，并为每个 finding 给出最小修复 patch。
```

## 7. 验收门禁

| 门禁 | 要求 |
|---|---|
| 编码完成 | `domain_core::character_combat_san_chase` 具有领域专名 command/event/service/repository/error 类型。 |
| 正史一致 | 正式事实只由 Event Store 记录，projection/cache/RAG 可重建。 |
| 权限安全 | OpenFGA/OPA/Tool Grant/Policy Gate 无绕过路径。 |
| 幂等并发 | idempotency_key 与 expected_version 有正反测试。 |
| 可观测性 | tracing span、metric、audit log 均带 correlation_id / causation_id。 |
| 测试 | fmt、clippy、unit、integration、contract、replay/golden 按模块适配通过。 |
| 文档 | API/Event/DB schema 与实现同步更新，并在 trace map 中关联 `CODEX-0310-02-DOMAIN-CORE-127e67bc59`。 |

## 8. 建议落地文件

本 Prompt 是 primary implementation；下列 Rust 文件由本 Prompt 唯一声明。

- `crates/trpg-domain-core/src/character_combat_san_chase.rs`
- `crates/trpg-domain-core/tests/character_combat_san_chase_contract_tests.rs`

### 输出路径一致性门禁

- `建议 crate` 必须保持为 `trpg-domain-core`。
- `建议 Rust module` 必须保持为 `domain_core::character_combat_san_chase`。
- Rust src 文件名必须等于 module 末段 `character_combat_san_chase`，不得使用源文档路径截断名。
- 不得同时建议 flat Rust module file 与 directory-style Rust module file。
