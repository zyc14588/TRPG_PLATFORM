> [V2.1 原始路径追踪副本]
> 原始路径：`codex-prompts/90-traceability/docs-implementation-90-traceability-per-file-code-ready-90-traceability-docs-implementation-00-index-manifest-source-boundary-bf6fec9fb1.v5-code-ready-0620446feb.codex-prompt.md`
> 筛选状态：`path-provenance-only`
> 当前状态：该原始路径因含旧版本、旧 hash、中间产物或历史命名片段，不作为 Codex 当前执行入口；当前执行必须使用 `docs/codex/00-index/CURRENT_NORMALIZED_PROMPT_EXECUTION_MAP.md`、`docs/codex/00-index/CURRENT_SAFE_MODULE_AND_OUTPUT_MAP.md` 与 `docs/codex/00-index/CURRENT_TOKEN_REWRITE_TABLE.md` 中的 current-safe 名称。
> 内容处理：本副本仅用于证明已遍历和可追溯；正文历史版本词、旧 module、旧输出路径和旧 hash 不得转写为工程实现。

# Codex 持久化施工提示词 — Manifest 源边界 — V5 可编码级实现拆分

> 生成日期：2026-06-30
> Prompt ID：`CODEX-0977-90-TRACEABILITY-0620446feb`
> 源文件：`docs/implementation/90-traceability/per-file-code-ready/90-traceability/docs-implementation-00-index-manifest-source-boundary-bf6fec9fb1.v5-code-ready.md`
> 源 SHA256：`c892c5b9ffcd97386b5997ae7f6bbfec7c95acc166ca63712027a0063a26972d`
> 源大小：17096 bytes / 417 lines
> 归档分类：`90-traceability` — Traceability / source audit / requirement-to-test mapping
> 建议 crate：`trpg-docs-governance`
> 建议 Rust module：`traceability::manifest_source_boundary`
> 任务类型：`traceability-maintenance`
> 施工边界：文档治理 / 追踪维护施工；只维护 Markdown、索引、矩阵、报告或验证清单，不创建业务域代码。
> 读取状态：已读取完整文件内容并提取施工锚点。

## 0. 严格修复后的任务类型与边界

- 任务类型：`traceability-maintenance`
- 输出归并角色：`documentation-or-traceability`。
- 权威 crate：`trpg-docs-governance`。
- 稳定 Rust module：`traceability::manifest_source_boundary`；module 名不得携带生成用 hash 后缀、源文件路径片段、V4/V5 施工版本片段或截断尾缀。
- Prompt ID、源 SHA256、Prompt 文件名中的 hash 只用于追踪，不得转写为代码、测试名、migration、event schema、NATS subject 或 metric label。
- 施工边界：文档治理 / 追踪维护施工；只维护 Markdown、索引、矩阵、报告或验证清单，不创建业务域代码。

## 0.1 Rust module 文件布局策略（第三次严格修复）

- 本任务不是业务 Rust 输出 owner。
- 文档治理、traceability、ops runbook 的非 primary 任务不得创建业务 Rust module。
- 如需记录代码影响，只能创建 Markdown 追踪、补充需求或治理说明。

## 0.2 输出路径一致性与语义归并策略（第五次严格修复）

- 本任务类型默认不创建业务 Rust 输出文件。
- 建议输出文件只允许落在 Markdown 文档、索引、矩阵、报告或追踪更新路径中。
- 禁止从源文档路径、旧 V5 中间文件名、截断路径或 hash 片段生成 Rust module 文件。
## 1. 持久化上下文

本提示词用于 Codex CLI / Codex Cloud / Codex IDE 的单文件或小批次施工。执行前先阅读：

```text
AGENTS.md
docs/codex/00-index/codex-persistent-context.md
docs/codex/00-index/codex-execution-map.md
本提示词文件
```

不得把本提示词当作一次性聊天建议；它是可复用的施工任务契约。Codex 在上下文压缩、换线程或分批执行后，必须重新载入本提示词和上面的持久化上下文。

## 2. 不可变施工原则

- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。

## 3. 源文档结构摘要

### 3.1 标题结构

- H1: Manifest 源边界 — V5 可编码级实现拆分
- H2: 1. 施工原则与不可变约束
- H2: 2. 源文档读取摘要
- H3: 标题结构摘录
- H3: 推荐提示词 / 深度拆分 / 调研方向摘录
- H2: 3. 模块边界
- H2: 4. Rust module / trait / struct / enum
- H2: 5. SQLx migration SQL
- H2: 6. API、WebSocket 与事件契约
- H3: OpenAPI / WebSocket / Event Schema
- H2: 7. Command -> Workflow -> Decision -> Event Store 流程
- H2: 8. 权限、可见性与事实来源传播
- H2: 9. 错误码、幂等键、重试与一致性
- H2: 10. 实现任务切片
- H2: 11. 测试、集成验证与最小 Rust POC
- H3: 测试、POC 与验收
- H2: 12. 本文件在 V5 文档组中的归档结论

### 3.2 代码块语言

```text
text×2, ##×5, rust×3, plain×3, sql×1, yaml×1, json×1, mermaid×1, |×1
```

### 3.3 源文档关键锚点

- ## 1. 施工原则与不可变约束
- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。
- - 检测到施工关键词：`Authority Contract`×1, `Event Store`×1, `Visibility`×1, `Fact Provenance`×1。
- - 处理结论：本文件内容已纳入 V5 可编码拆分，输出 Rust 类型、SQLx migration、契约、测试与 POC 任务。
- \| Rust module \| `traceability::manifest_source_boundary` \|
- \| 写入口 \| `/api/v1/docs/traceability/commands` \|
- \| Event Store 表 \| `traceability_events` \|
- \| NATS / Realtime \| `trpg.docs.source.processed, trpg.docs.audit.published` \|
- \| 正史边界 \| 只允许 Event Store 持久化正式事实；Projection、Cache、RAG index 均可重建。 \|
- ## 4. Rust module / trait / struct / enum
- use async_trait::async_trait;
- use sqlx::{PgPool, Postgres, Transaction};
- pub enum AuthorityMode { HumanKp, AiKp }
- pub enum VisibilityLabel { Public, Party, KeeperOnly, InvestigatorPrivate, SystemPrivate }
- pub struct FactProvenance {
- pub struct CommandEnvelope<T> {
- pub command_id: Uuid,
- pub idempotency_key: String,
- pub expected_version: i64,
- pub correlation_id: Uuid,
- pub causation_id: Option<Uuid>,

### 3.4 深度拆分 / 调研 / POC 摘录

- ### 推荐提示词 / 深度拆分 / 调研方向摘录
- - 本文件未显式提供推荐提示词或调研方向；本轮继承 `architecture/document-set.md` 的完成标准与 V5 默认可编码级输出清单。
- ## 11. 测试、集成验证与最小 Rust POC
- ### 测试、POC 与验收
- [source code fence: rust]
- #[tokio::test]
- async fn manifest_source_boundary_rejects_authority_contract_violation() {
- // Arrange: 构造 AI_KP campaign 下的人类 KP 写命令，或 HUMAN_KP 下的 AI 正式写命令。
- // Act: 调用 command handler。
- // Assert: 返回 TrpgError::AuthorityViolation，且 Event Store 无新增事件。
- }
- #[tokio::test]
- async fn manifest_source_boundary_keeps_visibility_and_fact_provenance_on_replay() {
- // Arrange: 写入含 KeeperOnly/SystemPrivate 的事件序列。
- // Act: 重建 projection / RAG index / websocket delta。
- // Assert: principal 不具备权限时不可见，projection hash 与 golden fixture 一致。
- }
- [source code fence: plain]
- \| 测试层级 \| 必须覆盖 \|
- \|---\|---\|
- \| Unit \| Authority Guard、Visibility lattice、Fact Provenance copy-on-write、错误码映射。 \|
- \| Integration \| SQLx migration、expected_version 冲突、idempotency key 重放、OpenFGA/OPA deny。 \|
- \| Contract \| OpenAPI DTO、Event JSON Schema、NATS subject、WebSocket delta。 \|
- \| Golden Scenario \| 从 Command 到 Event Store 再到 Projection 的可重放一致性。 \|
- \| POC 命令 \| `cargo test -p trpg-docs-governance --test manifest_source_boundary_poc -- --nocapture`。 \|
- 观测指标：`trpg_command_total{module="manifest_source_boundary"}`、`trpg_event_append_latency_ms`、`trpg_policy_deny_total`、`trpg_projection_lag_events`、`trpg_visibility_redaction_total`。

### 3.5 检测到的契约元素

| 元素 | 摘录 |
|---|---|
| Rust items | AuthorityMode, VisibilityLabel, FactProvenance, CommandEnvelope, DecisionEnvelope, TrpgError, RegisterSourceFilePayload, LinkRequirementToTestPayload, MarkLegacyDispositionPayload, PublishAuditReportPayload, ManifestSourceBoundaryBf6fec9fb1Command, SourceFileRegisteredPayload, RequirementLinkedToTestPayload |
| SQL / migration | traceability_events, manifest_source_boundary_outbox |
| API endpoint | /api/v1/docs/traceability/commands |
| NATS / realtime | trpg.docs.source.processed, trpg.docs.audit.published, trpg.manifest.source.boundary.bf6fec9fb1.v5.event.schema.json |
| 模板债务 | 未发现旧模板债务。 |

## 4. Codex 编码提示词（可直接复制）

```text
你是 Codex，正在执行文档治理 / 追踪维护任务。

任务 ID：CODEX-0977-90-TRACEABILITY-0620446feb
源文档：docs/implementation/90-traceability/per-file-code-ready/90-traceability/docs-implementation-00-index-manifest-source-boundary-bf6fec9fb1.v5-code-ready.md
目标分类：90-traceability — 追踪矩阵、审计、处理证据
任务类型：traceability-maintenance
输出归并角色：documentation-or-traceability

目标：
1. 维护 Markdown 文档、索引、矩阵、报告、验证清单、批次计划或追踪证据。
2. 不创建业务域代码、API handler、migration、NATS subject 或正式状态写入路径。
3. 保留 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance 与 Policy Gate 约束。

建议输出文件：
docs/codex/90-traceability/manifest_source_boundary.md

完成后输出：
- 变更文档清单。
- 更新的索引 / 矩阵 / 报告条目。
- 未决问题和后续追踪项。
```

## 5. Codex 测试提示词（可直接复制）

```text
你是 Codex 文档校验代理，负责验证任务 CODEX-0977-90-TRACEABILITY-0620446feb 的文档治理结果。

检查项：
- Markdown 链接、表格、Prompt ID、源 SHA、路径均自洽。
- 不引入业务 Rust 输出路径。
- 不削弱施工原则。
- 若更新 batch / manifest / index，确保计数与实际文件一致。
```

## 6. Codex Review / Repair 提示词

```text
审查文档治理任务 CODEX-0977-90-TRACEABILITY-0620446feb：
1. 查找 Manifest、index、batch、validation 的计数不一致。
2. 查找旧版本报告是否未归档或误导为当前验收入口。
3. 查找 source-path-like Rust 输出路径或 hash 进入代码命名的风险。
4. 输出 P0/P1/P2 findings 和最小文档修复建议。
```

## 7. 验收门禁

| 门禁 | 要求 |
|---|---|
| 文档自洽 | 索引、路径、Prompt ID、源 SHA 与 Manifest 一致。 |
| 无代码越界 | 不声明业务 Rust src/test 输出文件。 |
| 约束保留 | 不削弱 Authority、KP、Event Store、Visibility、Provenance、Policy Gate。 |

## 8. 建议落地文件

本 Prompt 只维护文档治理或追踪材料。

- `docs/codex/90-traceability/manifest_source_boundary.md`
