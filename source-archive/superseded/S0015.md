
# Superseded V2 Manifests

本目录保存 v2 包的旧 manifest / validation / build summary，仅用于 provenance。它们不是 v2.1 当前验收入口，不得被 Codex 用作当前包文件计数、hash 或通过结论。

当前 manifest 只以根目录 `MANIFEST.md` 与 `manifests/SELF_CONTAINED_PACKAGE_MANIFEST.md` 为准。
