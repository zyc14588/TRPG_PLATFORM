# Legacy Source Provenance — Not Active Execution Input

> v2.1 strict note: this file preserves an original V6 Codex input path after the active prompt path was normalized. It is read-only provenance. Do not execute module/output names from this file.

# Codex 持久化施工提示词 — NATS JetStream 事件总线详细设计 — V5 可编码级实现拆分

> 生成日期：2026-06-30
> Prompt ID：`CODEX-0680-06-DATA-EVENTING-6f2b9615a9`
> 源文件：`docs/implementation/90-traceability/per-file-code-ready/06-data-eventing/sources-v3-baseline-document-group-docs-platform-event-bus-nats-6ed5a8e00d.v5-code-ready.md`
> 源 SHA256：`d993416f7bd53123d586a82a1fa814d1e966717b8e5982bab903eee4e6d52c84`
> 源大小：19411 bytes / 496 lines
> 归档分类：`06-data-eventing` — Event Store / SQLx / Outbox / NATS / Projection
> 建议 crate：`trpg-data-eventing`
> 建议 Rust module：`data_eventing::event_bus_nats`
> 任务类型：`product-code`
> 施工边界：supplemental 需求归并施工；只生成补充约束 Markdown，不创建 Rust src/test 输出。
> 读取状态：已读取完整文件内容并提取施工锚点。

## 0. 严格修复后的任务类型与边界

- 任务类型：`product-code`
- 输出归并角色：`supplemental-requirement`。
- 权威 crate：`trpg-data-eventing`。
- 稳定 Rust module：`data_eventing::event_bus_nats`；module 名不得携带生成用 hash 后缀、源文件路径片段、V4/V5 施工版本片段或截断尾缀。
- Prompt ID、源 SHA256、Prompt 文件名中的 hash 只用于追踪，不得转写为代码、测试名、migration、event schema、NATS subject 或 metric label。
- 施工边界：supplemental 需求归并施工；只生成补充约束 Markdown，不创建 Rust src/test 输出。

## 0.1 Rust module 文件布局策略（第三次严格修复）

- 本任务不是 Rust 输出 owner；Rust 文件布局由 primary prompt 决定。
- 本任务不得声明 flat module 或 directory mod module 输出文件。
- 需要影响代码时，只能写入 merge instruction，由 primary prompt 执行。
- 禁止把源文档路径、旧 V5 中间文件名、截断路径、版本流水或 hash 片段转写为 Rust 输出文件。

## 0.2 输出路径一致性与语义归并策略（第五次严格修复）

- Primary implementation prompt：`CODEX-0059-06-DATA-EVENTING-8ceec1d689`。
- 共享实现 module：`data_eventing::event_bus_nats`。
- 本 Prompt 只维护补充需求文件：`codex-prompts/06-data-eventing/P0105.md`。
- 需要影响代码时，只能把约束归并给 Primary Prompt：`CODEX-0059-06-DATA-EVENTING-8ceec1d689`；不得在本 Prompt 中创建或修改 Rust src/test 文件。
- 禁止从源文档路径、旧 V5 中间文件名、截断路径、版本流水或 hash 片段生成 Rust 输出文件。

## 0.3 Supplemental 语义一致性策略（第五次严格修复）

- `supplemental-requirement` Prompt 只能产生补充约束 Markdown，不得包含创建或实现 Rust module 的可执行指令。
- Supplemental 需要影响代码时，必须写明归并目标 Primary Prompt，并把代码修改交由 primary prompt 执行。
- Primary Prompt 是唯一可以声明 concrete Rust src/test 输出路径的 Prompt。
- 最终校验必须同时检查输出路径、正文施工语义、source-path-like 名称和短 hash 残片。
## 1. 持久化上下文

本提示词用于 Codex CLI / Codex Cloud / Codex IDE 的单文件或小批次施工。执行前先阅读：

```text
AGENTS.md
docs/codex/00-index/codex-persistent-context.md
docs/codex/00-index/codex-execution-map.md
本提示词文件
```

不得把本提示词当作一次性聊天建议；它是可复用的施工任务契约。Codex 在上下文压缩、换线程或分批执行后，必须重新载入本提示词和上面的持久化上下文。

## 2. 不可变施工原则

- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。

## 3. 源文档结构摘要

### 3.1 标题结构

- H1: NATS JetStream 事件总线详细设计 — V5 可编码级实现拆分
- H2: 1. 施工原则与不可变约束
- H2: 2. 源文档读取摘要
- H3: 标题结构摘录
- H3: 推荐提示词 / 深度拆分 / 调研方向摘录
- H4: 摘录 1
- H4: 摘录 2
- H4: 摘录 3
- H4: 摘录 4
- H2: 参考来源
- H2: 3. 模块边界
- H2: 4. Rust module / trait / struct / enum
- H2: 5. SQLx migration SQL
- H2: 6. API、WebSocket 与事件契约
- H3: OpenAPI / WebSocket / Event Schema
- H2: 7. Command -> Workflow -> Decision -> Event Store 流程
- H2: 8. 权限、可见性与事实来源传播
- H2: 9. 错误码、幂等键、重试与一致性
- H2: 10. 实现任务切片
- H2: 11. 测试、集成验证与最小 Rust POC

### 3.2 代码块语言

```text
text×6, ##×6, ####×3, rust×3, plain×3, sql×1, yaml×1, json×1, mermaid×1, |×1
```

### 3.3 源文档关键锚点

- # NATS JetStream 事件总线详细设计 — V5 可编码级实现拆分
- > 输入文件：`sources/v3-baseline-document-group/docs/platform/event-bus-nats.md`
- ## 1. 施工原则与不可变约束
- Authority Contract 不可变；变更只能 Fork 或新建 authority_contract_version。
- HUMAN_KP / AI_KP 在 Campaign 级互斥，任何 handler 都不得在同一 campaign 同时接受两种 KP authority。
- AI 不直接写正式状态；AI 只能提出 Proposal / ToolCall / DraftDecision。
- 正式状态必须走 Command -> Workflow -> Decision -> Event Store -> Projection。
- Event Store 是正史，Projection / Cache / RAG Index 均为可重建读模型。
- Visibility Label 与 Fact Provenance 必须跨 API、Event、Agent、RAG、Export、Log、Metric 传播。
- Tool Grant、Policy Gate、OpenFGA、OPA、Audit Log 不得被 Agent、插件或外部 Provider 绕过。
- 所有写命令必须具备 idempotency_key、expected_version、actor、correlation_id、causation_id。
- - 标题：NATS JetStream 事件总线详细设计
- - 检测到施工关键词：`Authority Contract`×1, `Event Store`×1, `Visibility`×1, `Fact Provenance`×1, `Agent Governance`×1, `OpenFGA`×2, `OPA`×2, `NATS`×11, `JetStream`×5, `SQLx`×3, `pgvector`×3, `Golden Scenario`×1。
- - 处理结论：本文件内容已纳入 V5 可编码拆分，输出 Rust 类型、SQLx migration、契约、测试与 POC 任务。
- - H1: NATS JetStream 事件总线详细设计
- - H2: 3. Outbox -> NATS
- - H2: 5. Rust Trait
- - H2: 6. JetStream 策略
- - H2: 8. 测试策略
- 你是资深 Rust 企业级架构师。请基于本文件继续拆分到实现级设计，输出：
- 1. 具体 Rust module / trait / struct / enum 定义；
- 2. SQLx migration SQL；
- 3. OpenAPI / WebSocket / Event Schema；
- 4. 单元测试、集成测试、Golden Scenario 测试用例；
- 5. 错误码、幂等键、重试策略与观测指标。
- 要求保持 Authority Contract、Visibility、Fact Provenance、Event Sourcing、Agent Governance 不变。
- - 对比 SQLx、SeaORM、Diesel 在当前模块中的适配成本。
- - 对比 NATS JetStream、Kafka、RabbitMQ 在事件投影与实时通知中的语义差异。

### 3.4 深度拆分 / 调研 / POC 摘录

- ### 推荐提示词 / 深度拆分 / 调研方向摘录
- #### 摘录 1
- ```text
- 推荐提示词：
- ~~~text
- 你是资深 Rust 企业级架构师。请基于本文件继续拆分到实现级设计，输出：
- 1. 具体 Rust module / trait / struct / enum 定义；
- 2. SQLx migration SQL；
- 3. OpenAPI / WebSocket / Event Schema；
- 4. 单元测试、集成测试、Golden Scenario 测试用例；
- 5. 错误码、幂等键、重试策略与观测指标。
- 要求保持 Authority Contract、Visibility、Fact Provenance、Event Sourcing、Agent Governance 不变。
- ~~~
- 进一步调研方向：
- - 对比 SQLx、SeaORM、Diesel 在当前模块中的适配成本。
- - 对比 NATS JetStream、Kafka、RabbitMQ 在事件投影与实时通知中的语义差异。
- - 验证 OpenFGA + OPA 的本地开发部署、缓存、灰度与回滚方案。
- - 为本模块生成最小可运行 Rust POC。
- ```
- ## 11. 测试、集成验证与最小 Rust POC
- ### 测试、POC 与验收
- ```rust
- #[tokio::test]
- async fn event_bus_nats_rejects_authority_contract_violation() {
- // Arrange: 构造 AI_KP campaign 下的人类 KP 写命令，或 HUMAN_KP 下的 AI 正式写命令。
- // Act: 调用 command handler。
- // Assert: 返回 TrpgError::AuthorityViolation，且 Event Store 无新增事件。
- }
- #[tokio::test]
- async fn event_bus_nats_keeps_visibility_and_fact_provenance_on_replay() {
- // Arrange: 写入含 KeeperOnly/SystemPrivate 的事件序列。
- // Act: 重建 projection / RAG index / websocket delta。
- // Assert: principal 不具备权限时不可见，projection hash 与 golden fixture 一致。
- }
- ```
- \| 测试层级 \| 必须覆盖 \|

### 3.5 检测到的契约元素

| 元素 | 摘录 |
|---|---|
| Rust items | AuthorityMode, VisibilityLabel, FactProvenance, CommandEnvelope, DecisionEnvelope, TrpgError, AppendEventPayload, PublishOutboxPayload, RebuildProjectionPayload, CreateSnapshotPayload, EventBusNats6ed5a8e00dCommand, EventAppendedPayload, OutboxPublishedPayload |
| SQL / migration | event_store, event_bus_nats_outbox |
| API endpoint | /api/v1/internal/event-store/append |
| NATS / realtime | trpg.events.appended, trpg.projection.rebuild.requested, trpg.event.bus.nats.6ed5a8e00d.v5.event.schema.json |
| 模板债务 | 未发现旧模板债务。 |

## 4. Codex 编码提示词（可直接复制）

```text
你是 Codex，正在执行 supplemental requirement 归并任务。

任务 ID：CODEX-0680-06-DATA-EVENTING-6f2b9615a9
源文档：docs/implementation/90-traceability/per-file-code-ready/06-data-eventing/sources-v3-baseline-document-group-docs-platform-event-bus-nats-6ed5a8e00d.v5-code-ready.md
目标分类：06-data-eventing — Event Store / SQLx / Outbox / NATS / Projection
权威 crate：trpg-data-eventing
归并目标：data_eventing::event_bus_nats
Primary Prompt：CODEX-0059-06-DATA-EVENTING-8ceec1d689
任务类型：product-code
输出归并角色：supplemental-requirement

目标：
1. 从源文档中提取对 `data_eventing::event_bus_nats` 有价值的补充约束、测试断言、验收项、错误码、观测指标、权限/可见性要求和风险说明。
2. 只维护补充需求 Markdown：`codex-prompts/06-data-eventing/P0105.md`。
3. 需要影响代码时，把约束归并给 Primary Prompt `CODEX-0059-06-DATA-EVENTING-8ceec1d689`，由 primary 任务统一处理 Rust 输出文件。
4. 不得声明、修改或要求新增 Rust src/test 输出文件；不得在本任务中生成 migration、API handler、NATS subject 或正式状态写入路径。

执行顺序：
1. 读取 AGENTS.md、docs/codex/00-index/codex-persistent-context.md、docs/codex/00-index/codex-execution-map.md，以及本任务提示词。
2. 阅读源文档锚点，只抽取补充约束，不执行代码变更。
3. 在 `codex-prompts/06-data-eventing/P0105.md` 中写入：约束摘要、必须合并到 primary 的事项、测试断言、风险、追踪引用、未决问题。
4. 在补充文档顶部声明 Primary Prompt：`CODEX-0059-06-DATA-EVENTING-8ceec1d689`，并声明本任务不拥有 Rust 输出路径。
5. 若发现 primary prompt 缺少必要施工约束，只在 supplemental Markdown 中记录 merge instruction，不直接编辑 primary Rust 文件。

建议输出文件：
codex-prompts/06-data-eventing/P0105.md

完成后输出：
- 补充需求文档路径。
- 需要 primary prompt 合并的约束清单。
- 建议新增或强化的测试断言清单。
- 未决问题和风险。
```

## 5. Codex 测试提示词（可直接复制）

```text
你是 Codex 测试代理，负责审查 supplemental 任务 CODEX-0680-06-DATA-EVENTING-6f2b9615a9 的补充需求是否可被 primary prompt 合并。

测试目标：
1. 验证 supplemental 文档没有声明 Rust src/test 输出路径。
2. 验证 supplemental 文档明确指向 Primary Prompt，并列出可合并约束。
3. 验证补充约束没有削弱 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance 或 Policy Gate。
4. 验证补充测试断言可以映射到 primary 的 contract / integration / replay / golden 测试。

必须执行或补齐的检查：
- grep 检查本 supplemental 文档不得出现 concrete Rust src/test 输出路径作为本任务输出。
- 检查 Primary Prompt ID、源文件、源 SHA 和本任务 ID 是否齐全。
- 检查补充约束是否以 P0/P1/P2 或 must/should/could 分级。
- 若与 primary 合并执行，再运行 primary prompt 指定的 cargo fmt / clippy / test。
```

## 6. Codex Review / Repair 提示词

```text
审查 supplemental 任务 CODEX-0680-06-DATA-EVENTING-6f2b9615a9：
1. 查找是否把 supplemental 误写成 primary 代码施工。
2. 查找是否声明 Rust src/test 输出文件、migration、API handler、NATS subject 或正式状态写入路径。
3. 查找是否缺少 Primary Prompt 归并目标。
4. 查找是否存在削弱施工原则的表述。
5. 若发现需要代码改动，只输出 merge instruction，交由 Primary Prompt 执行。
```

## 7. 验收门禁

| 门禁 | 要求 |
|---|---|
| 归并角色 | 本 Prompt 必须保持 `supplemental-requirement`。 |
| Primary 归属 | 所有实现约束必须归并到 `CODEX-0059-06-DATA-EVENTING-8ceec1d689`。 |
| 补充文档 | 必须维护 `codex-prompts/06-data-eventing/P0105.md`。 |
| 施工原则 | 不得弱化 Authority Contract、KP 互斥、Event Store 正史、Visibility、Fact Provenance 或 Policy Gate。 |
| 可追踪性 | supplemental 文档必须引用源文件 `docs/implementation/90-traceability/per-file-code-ready/06-data-eventing/sources-v3-baseline-document-group-docs-platform-event-bus-nats-6ed5a8e00d.v5-code-ready.md` 与 Prompt ID `CODEX-0680-06-DATA-EVENTING-6f2b9615a9`。 |
| 测试 | 只提交补充测试断言；实际代码测试由 primary prompt 执行。 |

## 8. 建议落地文件

本 Prompt 是 `CODEX-0059-06-DATA-EVENTING-8ceec1d689` 的 supplemental requirement；不得声明或创建 primary Rust 输出文件。
只维护下列补充需求追踪文档，或在与 primary prompt 合并执行时把约束并入 primary 实现。

- `codex-prompts/06-data-eventing/P0105.md`

### 输出路径一致性门禁

- `建议 crate` 必须保持为 `trpg-data-eventing`。
- `建议 Rust module` 必须保持为 `data_eventing::event_bus_nats`。
- 若需要影响 `data_eventing::event_bus_nats`，必须经 primary prompt `CODEX-0059-06-DATA-EVENTING-8ceec1d689` 归并。
- 本任务不拥有 Rust src/test 输出路径。
