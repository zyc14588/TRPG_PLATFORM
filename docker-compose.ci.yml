# CI uses the same real service graph and security boundaries as production.
# Test jobs supply disposable external secrets and policy-service identifiers;
# this file intentionally does not replace app binaries with fake health pages.
include:
  - path: compose.yml
