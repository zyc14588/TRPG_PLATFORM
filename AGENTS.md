# Codex repository entry

Before doing any work in this repository, read `.codex/SESSION_START.md` and follow it as the project-level session entry.

Use that entry to select exactly one mode: `PLAN`, `IMPLEMENT`, `ACCEPT`, or `REPAIR`, then continue only through the route it defines.
