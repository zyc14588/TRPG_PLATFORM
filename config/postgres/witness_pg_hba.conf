# The container is attached only to Compose's internal backend network.
# Runtime access is TLS-only and split between append/read and read-only
# principals. The owner is reserved for role bootstrap and schema migration.
# The CI security contract rejects an additional database network or
# production port.
local   all             all                                     scram-sha-256
hostnossl all           all             0.0.0.0/0               reject
hostnossl all           all             ::/0                    reject
hostssl coc_ai_trpg_witness trpg_witness_append_login samenet   scram-sha-256
hostssl coc_ai_trpg_witness trpg_witness_read_login samenet     scram-sha-256
hostssl coc_ai_trpg_witness trpg_witness_owner samenet          scram-sha-256
hostssl all             all             0.0.0.0/0               reject
hostssl all             all             ::/0                    reject
